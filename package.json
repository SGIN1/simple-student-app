{
  "name": "simple-student-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild components/CertificatePdfDocument.jsx --bundle --outfile=components/CertificatePdfDocument.js --format=cjs --platform=node",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@react-pdf/renderer": "^4.3.0",
    "mongodb": "^6.6.0",
    "qrcode": "^1.5.3",
    "react": "^19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}