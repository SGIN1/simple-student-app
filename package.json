{
  "name": "simple-student-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild api/generateCertificateTwo2.js --bundle --outfile=api/generateCertificateTwo2.js.build --format=cjs --platform=node",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.6.0",
    "qrcode": "^1.5.3",
    "react": "^19.1.0",
    "@vercel/og": "^0.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}